/target
*.DS_Store

# Large test fixtures — download with ./scripts/download-fixtures.sh
tests/fixtures/*.onnx
tests/fixtures/*.mlmodel
# Keep the mlpackage directory structure (it's small)
# Keep the README

# Swift Package Manager build artifacts
.build/
