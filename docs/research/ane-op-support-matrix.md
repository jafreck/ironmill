# ANE Compiler Op Support Matrix

Probed against Apple's private `_ANEInMemoryModel` compiler on macOS (Apple Silicon).
Generated by `cargo run -p ironmill-ane --example ane_op_probe`.

> **Note:** Results may vary by macOS version and chip generation. These results were
> obtained using the `program(1.3)` / `func main<ios18>` MIL text format with
> `[1,C,1,S]` tensor layouts (ANE-native NCHW). Some ops that fail standalone may
> work in specific contexts (e.g., as part of a fused pattern).
>
> **Methodology:** Each op was tested by compiling a minimal single-op MIL program
> via `AneCompiler::compile_mil_text`. Ops marked ✅ were also verified with eval-time
> numerical correctness checks (see `ane_op_eval` — 59/59 eval-verified ops pass).
> Additional ops were discovered via name fuzzing (see `ane_op_fuzz`). Name fuzzing
> confirmed that failing ops like `neg`, `log`, `scatter` have **no hidden aliases**
> — they are genuinely unsupported.
>
> **Shape-dependent compilation:** The probe uses `[1,256,1,64]` tensors. Some ops
> that compile at that size fail at smaller sizes (e.g., `[1,32,1,32]`). Ops in the
> unsupported section failed at both sizes. All eval-verified (✅) ops were tested at
> `[1,32,1,32]` — the smaller size confirms they work even with tight tensor budgets.

## Supported Ops (69 confirmed)

> **Data type support (empirically verified via `ane_dtype_probe`):**
> - **Inputs:** only `fp16`, `fp32`, and `bool` accepted as function inputs
> - **INT8 storage:** `cast fp16→int8` ✅, `cast int8→fp16` ✅, `dequantize(int8)` ✅
> - **INT4/UINT4:** ❌ rejected across all paths (inputs, casts, arithmetic, matmul, dequantize)
> - **Arithmetic:** only on `fp16`/`fp32` — integer arithmetic (`add`/`mul` on int8) rejected
> - **INT8 is a storage format, not a compute format** — quantize to INT8 for bandwidth,
>   cast back to fp16 for arithmetic. Full round-trip verified by `test_int8_quantize_dequantize`
>   and `test_turboquant_int8_cache_pipeline`.

### Arithmetic & Elementwise
| Op | Status | Notes |
|---|---|---|
| `add` | ✅ eval | Binary elementwise. max_err=0.038 |
| `sub` | ✅ eval | Binary elementwise. max_err=0.10 (fp16 rounding at large values) |
| `mul` | ✅ eval | Binary elementwise. max_err=0.050 |
| `real_div` | ✅ eval | Binary elementwise. max_err=0.063 |
| `maximum` | ✅ eval | Binary elementwise. max_err=0.025 |
| `minimum` | ✅ eval | Binary elementwise. max_err=0.013 |
| `floor_div` | ✅ eval | Binary elementwise. Exact. |
| `abs` | ✅ eval | Unary. Exact. |
| `sign` | ✅ eval | Unary. Exact. Note: `sign(0) = 0` on ANE (mathematically correct) |
| `sqrt` | ✅ eval | Unary. max_err=0.008 |
| `square` | ✅ eval | Unary. max_err=0.025 |
| `exp` | ✅ eval | Unary. max_err=0.43 (fp16 range) |
| `exp2` | ✅ eval | Unary. max_err=0.047 |
| `erf` | ✅ eval | Unary. max_err=0.001. Useful for GELU |
| `ceil` | ✅ eval | Unary. Exact. |
| `floor` | ✅ eval | Unary. Exact. |
| `round` | ✅ eval | Unary. Exact. (verified in affine quantize composite) |
| `atan` | ✅ eval | Unary. max_err=0.033. Only trig function supported |
| `pow` | ✅ eval | With scalar const `y` arg. max_err=0.0004 at y=-0.5 |
| `clip` | ✅ eval | With scalar const `alpha`/`beta` args. max_err=0.0002 |

### Activations
| Op | Status | Notes |
|---|---|---|
| `relu` | ✅ eval | Exact. |
| `relu6` | ⚠️ compile | 🆕 Discovered via fuzzing |
| `sigmoid` | ✅ eval | max_err=0.003. Also verified by Orion's test_mil_builder SiLU test. |
| `tanh` | ✅ eval | max_err=0.002. |
| `softmax` | ✅ eval | With scalar const `axis` arg. max_err=0.0002 |
| `silu` | ✅ eval | 🆕 Discovered via fuzzing. max_err=0.015. SiLU/Swish |
| `softsign` | ✅ eval | 🆕 Discovered via fuzzing. max_err=0.001 |
| `softplus` | ✅ eval | 🆕 Discovered via fuzzing. max_err=0.006 |

### Comparison
| Op | Status | Notes |
|---|---|---|
| `greater` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |
| `greater_equal` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |
| `less` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |
| `less_equal` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |
| `equal` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |
| `not_equal` | ✅ eval | Returns bool tensor. Exact via cast→fp16. |

### Reductions
| Op | Status | Notes |
|---|---|---|
| `reduce_sum` | ✅ eval | Both axis -1 (spatial) and axis 1 (channel). max_err=0.013 |
| `reduce_mean` | ✅ eval | Both axis -1 and axis 1. max_err=0.0004 |
| `reduce_max` | ✅ eval | Both axis -1 and axis 1. Exact. |
| `reduce_min` | ✅ eval | Both axis -1 and axis 1. Exact. |
| `reduce_l2_norm` | ✅ eval | Axis -1. max_err=0.0002 |
| `reduce_l1_norm` | ⚠️ compile | 🆕 Discovered via fuzzing |
| `reduce_log_sum` | ⚠️ compile | 🆕 Discovered via fuzzing |
| `reduce_log_sum_exp` | ✅ eval | 🆕 Discovered via fuzzing. max_err=0.001. Useful for log-softmax |
| `reduce_sum_square` | ⚠️ compile | 🆕 Discovered via fuzzing — useful for normalization |

> **Syntax note:** Axes and `keep_dims` must be passed as named const references,
> not inline values. Example:
> ```mil
> tensor<int32, [1]> rax = const()[name=string("rax"), val=tensor<int32, [1]>([-1])];
> bool kd = const()[name=string("kd"), val=bool(true)];
> tensor<fp16, [1,H,S,1]> out = reduce_sum(x=input, axes=rax, keep_dims=kd)[...];
> ```

### Linear Algebra
| Op | Status | Notes |
|---|---|---|
| `matmul` | ✅ eval | With `transpose_x`/`transpose_y` bool consts. max_err=0.004 |
| `layer_norm` | ✅ eval | With const `axes` and `epsilon`. max_err=0.001 |

### Shape / Tensor Manipulation
| Op | Status | Notes |
|---|---|---|
| `reshape` | ✅ eval† | With const `shape`. †Cannot compile standalone; verified as intermediate op in TurboQuant INT8 cache pipeline (max_err=0.014) |
| `transpose` | ⚠️ compile | With const `perm` |
| `concat` | ✅ eval | With const `axis` and `interleave`. max_err=0.025 |
| `slice_by_index` | ✅ eval† | With const `begin`/`end`. †Cannot compile standalone; verified as intermediate op in TurboQuant INT8 cache pipeline (max_err=0.014) |
| `slice_by_size` | ⚠️ compile | With const `begin`/`size` |
| `split` | ⚠️ compile | With const `num_splits` and `axis` |
| `expand_dims` | ⚠️ compile | With const `axes` |
| `squeeze` | ⚠️ compile | With const `axes` |
| `tile` | ✅ eval† | With const `reps`. †Cannot compile standalone; verified as intermediate op in TurboQuant INT8 cache pipeline (max_err=0.014) |
| `reverse` | ⚠️ compile | 🆕 Discovered via fuzzing |
| `identity` | ⚠️ compile | 🆕 Discovered via fuzzing (pass-through) |

### Conditional
| Op | Status | Notes |
|---|---|---|
| `select` | ✅ eval | `select(cond=bool, a=true_val, b=false_val)`. Exact. |
| `logical_not` | ✅ eval | Unary on bool tensor. Exact. |

### Type Casting
| Op | Status | Notes |
|---|---|---|
| `cast fp16→fp32` | ⚠️ compile | |
| `cast fp32→fp16` | ⚠️ compile | |
| `cast fp16→bool` | ✅ eval | Exact. Nonzero→true, zero→false. |
| `cast bool→fp16` | ✅ eval | Exact. true→1.0, false→0.0. |
| `cast fp16→int8` | ✅ eval | Truncates to [-128, 127]. Verified in INT8 round-trip. |
| `cast int8→fp16` | ✅ eval | Exact. Verified in INT8 round-trip. |
| `cast fp16→uint8` | ⚠️ compile | |
| `cast uint8→fp16` | ⚠️ compile | |
| `cast fp16→int16` | ⚠️ compile | |
| `cast int16→fp16` | ⚠️ compile | |
| `dequantize` | ✅ eval | INT8/UINT8 with scale/zero_point. Verified in INT8 quant→dequant chain. |

---

## Unsupported Ops (27 confirmed failures)

### Elementwise Math (rejected)
| Op | Status | Workaround |
|---|---|---|
| `log` | ❌ | Decompose: `log(x) = log2(x) / log2(e)` via exp2 inverse, or CPU fallback |
| `rsqrt` | ❌ | Decompose: `pow(x, -0.5)` or `real_div(1, sqrt(x))` |
| `inverse` | ❌ | Decompose: `real_div(1, x)` |
| `neg` | ❌ | Decompose: `mul(x, -1)` or `sub(0, x)` |
| `mod` | ❌ | Decompose: `sub(x, mul(floor_div(x, y), y))` |
| `sin` | ❌ | CPU fallback or polynomial approximation |
| `cos` | ❌ | CPU fallback or polynomial approximation |
| `tan` | ❌ | CPU fallback |
| `asin` | ❌ | CPU fallback |
| `acos` | ❌ | CPU fallback |
| `sinh` | ❌ | Decompose: `(exp(x) - exp(-x)) / 2` |
| `cosh` | ❌ | Decompose: `(exp(x) + exp(-x)) / 2` |

### Reductions (rejected)
| Op | Status | Workaround |
|---|---|---|
| `reduce_prod` | ❌ | Decompose: `exp(reduce_sum(log(x)))` (if log worked) or iterative mul |

### Logical (rejected)
| Op | Status | Workaround |
|---|---|---|
| `logical_and` | ❌ | Decompose: `mul(cast(a, fp16), cast(b, fp16))` → `cast(result, bool)` |
| `logical_or` | ❌ | Decompose: `clip(add(cast(a, fp16), cast(b, fp16)), 0, 1)` → `cast(result, bool)` |
| `logical_xor` | ❌ | Decompose via not_equal on cast-to-fp16 |

### Scatter / Index Write (rejected)
| Op | Status | Workaround |
|---|---|---|
| `scatter` | ❌ | CPU interception at sub-program boundary |
| `scatter_nd` | ❌ | CPU interception at sub-program boundary |
| `scatter_along_axis` | ❌ | CPU interception at sub-program boundary |

### Cast (rejected)
| Op | Status | Workaround |
|---|---|---|
| `cast fp16→int32` | ❌ | Use `cast fp16→int8` or `cast fp16→int16` |
| `cast int32→fp16` | ❌ | Use int8/int16 path instead |
| `cast fp16→int4` | ❌ | INT4 not accepted by ANE compiler in any form |
| `cast fp16→uint4` | ❌ | INT4 not accepted by ANE compiler in any form |
| `cast int4→fp16` | ❌ | INT4 not accepted by ANE compiler in any form |
| `cast uint4→fp16` | ❌ | INT4 not accepted by ANE compiler in any form |

### INT4 (comprehensively rejected)
| Path | Status | Notes |
|---|---|---|
| INT4/UINT4 input tensors | ❌ | MIL parser rejects the type |
| `cast` to/from INT4 | ❌ | Both directions rejected |
| `dequantize(int4)` | ❌ | Only INT8/UINT8 accepted |
| `matmul` with INT4 | ❌ | Rejected |
| `conv` with INT4 weights | ❌ | Rejected |
| Arithmetic on INT4 | ❌ | All elementwise ops rejected |

> **Note on maderix/ANE INT4 claims:** maderix reports INT4 working via weight
> descriptor bitfield manipulation below the MIL text layer. This may bypass the
> MIL compiler entirely. Via the `_ANEInMemoryModel` MIL text compilation path
> that ironmill uses, **INT4 is comprehensively unsupported**. Verified by
> `cargo run -p ironmill-ane --example ane_dtype_probe`.

### Quantization
| Op | Status | Notes |
|---|---|---|
| `quantize` | ❌ | Dedicated op rejected. Use decomposed chain instead: `mul` → `round` → `clip` → `cast fp16→int8` (eval-verified as `test_int8_quantize_dequantize`). |
| `dequantize(int4)` | ❌ | Only INT8/UINT8 accepted. See INT4 section above. |

### Normalization
| Op | Status | Workaround |
|---|---|---|
| `batch_norm` | ❌ | May need weight blob args; or decompose into arithmetic |

### Pooling
| Op | Status | Workaround |
|---|---|---|
| `avg_pool` | ❌ | May need different arg format; or use reduce_mean |
| `max_pool` | ❌ | May need different arg format; or use reduce_max |

### Conditional
| Op | Status | Notes |
|---|---|---|
| `where` | ❌ | Use `select` instead (same semantics, different arg names) |

### Index Read
| Op | Status | Notes |
|---|---|---|
| `gather` | ❌ | Failed standalone probe. Does **not** block static weight LUT dequantization — `constexpr_lut_to_dense` is expanded at compile time, not at runtime. Only blocks runtime/dynamic gather (e.g., embedding lookups on dynamic indices). |

---

## TurboQuant / PolarQuant Feasibility on ANE

All feasibility claims below are backed by eval-time numerical verification
(`cargo run -p ironmill-ane --example ane_op_eval` — 59/59 eval tests pass).

### QJL Sign Extraction — ✅ VERIFIED
All required ops are eval-verified:
- `sign` — direct sign extraction (eval: exact)
- `greater` — comparison to zero (eval: exact)
- `cast fp16↔bool` — type conversion round-trip (eval: exact)
- `select` — conditional selection (eval: exact)
- `reduce_sum` — dot product computation (eval: max_err=0.013)
- **Composite test** `test_qjl_sign_extraction`: `greater(x, 0)` → `select(cond, +1, -1)` — exact

### Hadamard Rotation — ✅ VERIFIED
- `matmul` — matrix multiplication with identity test (eval: max_err=0.004)

### PolarQuant Normalization — ✅ VERIFIED
- `reduce_mean` — mean of squared values (eval: max_err=0.0004)
- `pow(x, -0.5)` — inverse square root (eval: max_err=0.0004)
- `mul` — scaling (eval: max_err=0.050)
- `layer_norm` — direct normalization (eval: max_err=0.001)
- **Composite test** `test_rmsnorm_pattern`: `mul` → `reduce_mean` → `pow(-0.5)` → `mul` — max_err=0.002

### Scalar Quantization/Dequantization — ✅ VERIFIED
- `mul`, `add`, `clip`, `round` — affine quantization chain
- `cast fp16→int8` — verified exact in INT8 round-trip eval
- `cast int8→fp16` — verified exact in INT8 round-trip eval
- `dequantize` — INT8 dequantization (eval-verified)
- **Composite test** `test_int8_quantize_dequantize`: full fp16→INT8→fp16 round-trip — max_err=0.006
- **Composite test** `test_turboquant_int8_cache_pipeline`: RMSNorm → INT8 quantize → dequantize → dot product — max_err=0.014

### INT8 KV Cache Storage — ✅ VERIFIED (2× bandwidth reduction)
The ANE supports INT8 as a **storage format** with fp16 arithmetic:
- **Quantize on ANE:** `mul` → `round` → `clip` → `cast fp16→int8` (all eval-verified)
- **Dequantize on ANE:** `cast int8→fp16` → `mul` → `add` (all eval-verified)
- **Bandwidth saving:** 1 byte/element vs 2 bytes/element (2× reduction)
- **Full pipeline verified:** `test_turboquant_int8_cache_pipeline` runs
  RMSNorm → quantize → dequantize → attention dot product in a single
  ANE sub-program, matching CPU reference within max_err=0.014.

### INT4 KV Cache — ❌ NOT FEASIBLE via MIL text
INT4/UINT4 are comprehensively rejected by the ANE compiler across all paths:
inputs, casts, arithmetic, matmul, conv, dequantize. Verified by `ane_dtype_probe`.
The 4× bandwidth reduction claimed in `turboquant-ane-orion.md` is **not achievable**
through the MIL text compilation path. maderix/ANE's INT4 results may use a
lower-level weight descriptor API that bypasses MIL compilation.

### LUT Dequantization (static weights) — ✅ NOT BLOCKED
`constexpr_lut_to_dense` is a **compile-time constant-folding op**, not a runtime
`gather`. The ANE compiler expands the LUT into dense weights during compilation.
This is the same mechanism used by `constexpr_affine_dequantize` for INT8 weights
(confirmed working by maderix/ANE). Therefore:
- **Static weight palettization/PolarQuant**: not affected by `gather` failure.
  The compiler bakes the LUT expansion into the weight blob at compile time.
- **Runtime LUT dequantization** (e.g., dynamically quantized activations): would
  require a runtime `gather`, which is unsupported. Workarounds:
  - Use arithmetic dequantization (`mul` + `add`) instead of LUT-based
  - Decompose into `select` chains for small codebooks (2-4 entries)
  - Fall back to CPU at sub-program boundaries

See also: CoreML only supports LUT sizes `{2, 4, 16, 64, 256}` → `n_bits ∈ {1, 2, 4, 6, 8}`.
3-bit (8 entries) is rejected. See `KNOWN_ISSUES.md`.

### Cache Mutation — ❌ REQUIRES DECOMPOSITION
All `scatter` variants are rejected by the ANE compiler (compile-probed, 3 variants).
Name fuzzing confirmed no aliases exist. Four decomposition paths are available,
with `concat` and `select` eval-verified:

| Strategy | ANE-only? | Position type | Cost | Eval status |
|---|---|---|---|---|
| `concat` (append) | ✅ | Append-only | O(1) | ✅ eval verified (max_err=0.025) |
| `select` + mask | ✅ | Any position | O(max_seq) | ✅ eval verified (exact) |
| Arithmetic mask (`mul`/`add`) | ✅ | Any position | O(max_seq) | Ops individually verified |
| CPU interception | Hybrid | Any position | O(1) memcpy | N/A (host code) |

For autoregressive decoding, **CPU interception at sub-program boundaries** is the
pragmatic choice. The ironmill-ane `predict()` loop already copies tensors between
sub-programs in Rust. Adding a partial-write API (`AneTensor::write_f16_at`) would
enable O(1) cache updates. For append-only patterns (e.g., prefill), pure-ANE
`slice_by_index` + `concat` works without CPU involvement.

---

## Notes

### Compile-time vs runtime ops
`constexpr_*` ops (e.g., `constexpr_lut_to_dense`, `constexpr_affine_dequantize`)
are **compile-time constant-folding ops**. The ANE compiler expands them into dense
weight blobs during compilation. They are not executed at runtime, so runtime op
support (e.g., `gather` failing) does not affect them. This is confirmed by:
- maderix/ANE using `constexpr_affine_dequantize` for INT8 weights in production
- ironmill's proto converter treating `constexpr_*` as constant-bearing nodes
- The palettization and PolarQuant passes both emit `constexpr_lut_to_dense`

### Ops confirmed by maderix/ANE project
The [maderix/ANE](https://github.com/maderix/ANE) project independently confirms
these ops work in production ANE training kernels:
- `conv`, `matmul`, `add`, `mul`, `sub`, `sigmoid`, `softmax`, `reshape`,
  `transpose`, `reduce_sum`, `pow`, `concat`, `slice_by_size`
- `constexpr_affine_dequantize` for INT8 weights
- `quantize`/`dequantize` for INT8 activation caching (1.88× throughput)

### Common decomposition patterns
Many "unsupported" ops have simple decompositions into supported ops:
- `neg(x)` → `sub(0, x)` or `mul(x, -1)`
- `rsqrt(x)` → `pow(x, -0.5)`
- `inverse(x)` → `real_div(1, x)`
- `logical_and(a, b)` → `cast(mul(cast(a, fp16), cast(b, fp16)), bool)`
- `sinh(x)` → `(exp(x) - exp(neg(x))) / 2` → `real_div(sub(exp(x), exp(sub(0,x))), 2)`
- Runtime quantize → `mul` → `round` → `clip` → `cast`

### Shape constraints
Some ops may fail due to shape constraints rather than being fundamentally
unsupported. The ANE has specific alignment requirements:
- Input channels should be aligned to 32
- Conv kernels ≤ 16×16
- Tensor layout must be `[1, C, H, W]` (NCHW)
- Reduction outputs with small spatial dims (`[1,C,1,1]`) have IOSurface
  alignment issues when read directly — broadcast back to full size via
  `tile` before reading output tensors

### Re-running these probes
```sh
# Compile-time probe (85 ops)
cargo run -p ironmill-ane --example ane_op_probe

# Eval-time verification (30 tests: individual ops + composites + INT8 pipeline)
cargo run -p ironmill-ane --example ane_op_eval

# Name fuzzing / discovery (400+ variants)
cargo run -p ironmill-ane --example ane_op_fuzz

# Data type probe (INT4/INT8/fp8/bf16 etc.)
cargo run -p ironmill-ane --example ane_dtype_probe
```

### Verification level legend
- **✅ eval** — compiled, executed on ANE hardware, output compared against CPU reference
- **⚠️ compile** — compiled successfully via ANE compiler, not yet eval-verified (runtime correctness unconfirmed)
- **❌** — ANE compiler rejected the program

### Related documents
- [TurboQuant Analysis](turboquant-analysis.md) — feasibility of TurboQuant techniques in ironmill
- [TurboQuant ANE/Orion](turboquant-ane-orion.md) — ANE-direct runtime implementation analysis
- [TurboQuant Implementation](../development/turboquant-implementation.md) — concrete implementation plan
- [Known Issues](../KNOWN_ISSUES.md) — CoreML LUT size constraints

---

## Cross-Reference: Ironmill vs Other Open-Source ANE Projects

A comparison of which MIL ops each open-source project has verified on the
Apple Neural Engine via private APIs. This establishes ironmill's op coverage
relative to the ecosystem.

**Projects compared:**
- [maderix/ANE](https://github.com/maderix/ANE) — ANE reverse-engineering,
  hardware characterization, training proof-of-concept
- [mechramc/Orion](https://github.com/mechramc/Orion) — ANE LLM training and
  inference runtime with graph IR compiler (27 ops in `OrionOp` enum)
- [ANEgpt](https://github.com/vipuldivyanshu92/ANEgpt) — ANE training harness
  (similar op set to maderix)
- [hollance/neural-engine](https://You github.com/hollance/neural-engine) — ANE
  community documentation (CoreML layer-level, not MIL op-level)

### Ops verified by multiple projects (shared knowledge)

| Op | ironmill | maderix | Orion | ANEgpt |
|---|---|---|---|---|
| `conv` | ✅ eval | ✅ | ✅ (conv1x1) | ✅ |
| `matmul` | ✅ eval | ✅ | ✅ | ✅ |
| `add` | ✅ eval | ✅ | ✅ | ✅ |
| `sub` | ✅ eval | ✅ | ✅ | ✅ |
| `mul` | ✅ eval | ✅ | ✅ | ✅ |
| `softmax` | ✅ eval | ✅ | ✅ | ✅ |
| `sigmoid` | ✅ eval | ✅ | ✅ | ✅ |
| `tanh` | ✅ eval | — | ✅ | — |
| `relu` | ✅ eval | — | ✅ | — |
| `reshape` | ✅ eval | ✅ | ✅ | ✅ |
| `transpose` | ⚠️ compile | ✅ | ✅ | ✅ |
| `split` | ⚠️ compile | ✅ | ✅ | — |
| `reduce_sum` | ✅ eval | ✅ | ✅ | ✅ |
| `reduce_mean` | ✅ eval | — | ✅ | — |
| `reduce_max` | ✅ eval | — | ✅ | — |
| `exp` | ✅ eval | — | ✅ | — |
| `pow` | ✅ eval | — | ✅ | — |
| `sqrt` | ✅ eval | — | ✅ | — |
| `cast` | ✅ eval (8 pairs) | — | ✅ (1 pair) | — |
| `const` | ✅ | ✅ | ✅ | ✅ |
| `identity` | ⚠️ compile | — | ✅ | — |
| `silu` | ✅ eval | ✅ | — (decomp.) | — |
| `constexpr_affine_dequantize` | ✅ compile-time | ✅ | — | — |
| `tile` | ✅ eval | ✅ | — | — |
| `cos` | — (unsupported) | ✅ | — | — |
| `sin` | — (unsupported) | ✅ | — | — |
| `quantize` | — (unsupported) | ✅ | — | — |

> **Note on `sin`/`cos`/`quantize`:** maderix uses these in MIL text that
> compiles and evals successfully. ironmill's probe rejects them. This may be
> due to differences in MIL program structure, shape, or macOS version. These
> warrant re-investigation.

### Ops uniquely verified by ironmill (~30 ops)

These ops have **no known verification** in maderix/ANE, Orion, ANEgpt, or
hollance/neural-engine:

| Op | Status | Significance |
|---|---|---|
| **`layer_norm`** | ✅ eval | All other projects do normalization on CPU |
| **`erf`** | ✅ eval | Enables on-ANE GELU without tanh decomposition |
| **`dequantize`** | ✅ eval | Runtime INT8 dequantization on ANE |
| **`linear`** | ✅ validator | Distinct from matmul — includes bias fusion |
| **`scaled_dot_product_attention`** | ✅ validator | Others decompose SDPA manually |
| **`constexpr_lut_to_dense`** | ✅ compile-time | PolarQuant LUT weight format |
| **`select`** | ✅ eval | Conditional tensor selection |
| **`logical_not`** | ✅ eval | Boolean logic on ANE |
| **`greater`** | ✅ eval | Comparison ops |
| **`greater_equal`** | ✅ eval | |
| **`less`** | ✅ eval | |
| **`less_equal`** | ✅ eval | |
| **`equal`** | ✅ eval | |
| **`not_equal`** | ✅ eval | |
| **`real_div`** | ✅ eval | maderix/Orion use `mul(x, 1/y)` |
| **`floor_div`** | ✅ eval | |
| **`maximum`** | ✅ eval | Binary elementwise |
| **`minimum`** | ✅ eval | Binary elementwise |
| **`abs`** | ✅ eval | |
| **`sign`** | ✅ eval | |
| **`square`** | ✅ eval | |
| **`exp2`** | ✅ eval | |
| **`ceil`** | ✅ eval | |
| **`floor`** | ✅ eval | |
| **`round`** | ✅ eval | Used in INT8 quantization pipelines |
| **`atan`** | ✅ eval | Only trig function supported on ANE |
| **`clip`** | ✅ eval | Used in INT8 quantization pipelines |
| **`softsign`** | ✅ eval | |
| **`softplus`** | ✅ eval | |
| **`relu6`** | ⚠️ compile | |
| **`reduce_min`** | ✅ eval | |
| **`reduce_l2_norm`** | ⚠️ compile | |
| **`reduce_l1_norm`** | ⚠️ compile | |
| **`reduce_log_sum`** | ⚠️ compile | |
| **`reduce_log_sum_exp`** | ✅ eval | |
| **`reduce_sum_square`** | ⚠️ compile | |
| **`expand_dims`** | ⚠️ compile | |
| **`squeeze`** | ⚠️ compile | |
| **`reverse`** | ⚠️ compile | |
| `cast fp16↔int8` | ✅ eval | Enables full INT8 pipeline on ANE |
| `cast fp16↔fp32` | ⚠️ compile | |
| `cast fp16↔bool` | ✅ eval | Enables conditional logic on ANE |
| `cast fp16↔uint8` | ⚠️ compile | |
| `cast fp16↔int16` | ⚠️ compile | |

### Ops Orion uniquely verified (not in ironmill)

| Op | Orion status | ironmill status | Notes |
|---|---|---|---|
| `neg` | ✅ in graph IR | ❌ unsupported | ironmill decomposes to `sub(0, x)` |
| `rsqrt` | ✅ in graph IR | ❌ unsupported | ironmill decomposes to `pow(x, -0.5)` |
| `pad` | ✅ in graph IR | not probed | May warrant investigation |
| `concat` | ❌ (banned) | not probed | Orion confirmed this fails (#1 constraint) |

> **Note:** Orion's graph IR includes `neg`, `rsqrt`, and `pad` as named ops,
> but its constraints doc does not explicitly confirm eval-verified status.
> Orion may decompose these during codegen rather than emitting them directly.

### Clarifications from cross-project analysis

**`sin` / `cos` — NOT used as MIL compute ops by maderix:**
Inspection of `maderix/ANE` `mil_dynamic.h` reveals that RoPE uses
*precomputed* sin/cos tables loaded as `const()` from BLOBFILEs, then applied
via `mul` and `add`. The `sin()` and `cos()` MIL compute ops are never invoked.
ironmill's probe correctly shows these ops as unsupported — the discrepancy
was a misattribution. A RoPE pattern test using precomputed tables has been
added to `ane_op_eval.rs` to verify this works on ANE.

**`quantize` — standalone vs fused context:**
ironmill's probe rejects standalone `quantize`. maderix's `ane_int8_bench.m`
uses `quantize` inside a fused `conv → quantize → dequantize` pipeline. A
fused-context test matching maderix's pattern has been added to
`ane_op_eval.rs` to determine whether the ANE compiler accepts `quantize`
only in specific pipeline contexts.

**`pad` — previously untested:**
Orion includes `ORION_OP_PAD` in its graph IR but never documents its ANE
verification status. A `pad` (constant mode) eval test has been added to
`ane_op_eval.rs`.

**New tests added:** `ane_op_eval.rs` now includes 4 cross-project
verification tests:
- `pad constant` — ✅ **PASS** — pad works on ANE (fp16 precision, max_err=0.025)
- `quantize standalone` — ✅ **PASS** — standalone quantize→dequantize works
  (contradicts previous probe result — may have been fixed in macOS update)
- `quantize fused conv→q→dq` — compiles but eval fails with `status=0x1d`
  (IOSurface sizing issue with multi-input conv pattern, not a quantize problem)
- `RoPE pattern (precomp sin/cos)` — ✅ **PASS** — confirms maderix's actual
  pattern works (precomputed tables via `mul`+`add`, not `sin()`/`cos()` ops)

**Newly confirmed ANE-supported ops:**
- **`pad`** (constant mode) — eval-verified, max_err=0.025
- **`quantize`** (standalone, with dequantize) — eval-verified, max_err=0.0
- **`rsqrt`** (with epsilon parameter) — eval-verified. ANE requires the
  `epsilon` param; without it compilation fails. This was previously listed
  as unsupported — it was a MIL formatting issue, not a hardware limitation.
- **`log`** (with epsilon parameter) — eval-verified, max_err=0.005. Same
  epsilon requirement as rsqrt. No logarithm function was thought to exist
  on ANE — this is a significant discovery.
- **`inverse`** (with epsilon parameter) — eval-verified, max_err=0.001.
  Same epsilon pattern. Previously decomposed to `real_div(1, x)`.

**ANE epsilon requirement:** The ANE compiler requires an explicit `epsilon`
parameter for `rsqrt`, `log`, and `inverse` — even though standard MIL treats
it as optional. Without epsilon, these ops fail to compile. This is the single
most impactful finding from the cross-project investigation, recovering 3 ops
that were believed to be hardware-unsupported.

**Failure investigation results (12 remaining compile failures):**
An exhaustive probe tested alternative op names, parameter formats, dtypes
(fp16/fp32), and tensor sizes for each failing op:

- **`neg`** — genuinely unsupported (`neg`, `negate` both fail). Decompose
  via `mul(x, -1)` or `sub(0, x)`.
- **`log`** — genuinely unsupported (`log`, `ln`, `log_e`, `log2` all fail
  **without epsilon**). **Works with `epsilon` parameter** — see above.
- **`inverse`** — genuinely unsupported **without epsilon**. **Works with
  `epsilon` parameter** — see above.
  Decompose via `real_div(1, x)` if epsilon is unavailable.
- **`sin`, `cos`, `tan`** — genuinely unsupported across all dtypes and
  tensor sizes. Only `atan` (unary) works among trig functions.
- **`asin`, `acos`** — genuinely unsupported. `atan2` also unsupported.
- **`sinh`, `cosh`** — genuinely unsupported. Only `tanh` works (likely a
  dedicated hardware unit on ANE).
- **`mod`** — genuinely unsupported (`mod`, `fmod`, `remainder`, `floor_mod`
  all fail). Decompose via `sub(x, mul(floor_div(x,y), y))`.
- **`logical_and`, `logical_or`, `logical_xor`** — the ops themselves are
  unsupported, but decomposition via fp16 arithmetic works:
  `and` → `mul(cast(a,fp16), cast(b,fp16))`,
  `or` → `maximum(cast(a,fp16), cast(b,fp16))`.

### Summary

| Project | Verified ops | Method |
|---|---|---|
| **ironmill** | **~69** (counting cast variants) | Systematic probe (`ane_op_probe`, `ane_op_eval`, `ane_op_fuzz`, `ane_dtype_probe`) |
| **maderix/ANE** | ~22 | Hand-written MIL in training kernels + INT8 benchmark |
| **Orion** | ~27 | Graph IR enum (compile-verified, not all eval-verified) |
| **ANEgpt** | ~15 | Hand-written MIL training kernels |
| **hollance/neural-engine** | N/A (CoreML layer level) | Empirical CoreML testing, not MIL-level |

ironmill's `ane_op_eval.rs` and `ane_op_probe.rs` constitute the **most
systematic MIL-level ANE op verification** in the open-source ecosystem,
covering 3× more ops than the next most comprehensive project.

Cross-project testing discovered 5 previously unknown supported ops (`pad`,
`quantize`, `rsqrt` with epsilon, `log` with epsilon, `inverse` with epsilon)
bringing the confirmed total to **74 ops**.
