//! Re-exports from [`ironmill_core::ane::mil_text`].
//!
//! The MIL text emitter was moved to `ironmill-core` so that both
//! `ironmill-compile` and `ironmill-inference` can use it without a
//! circular dependency.

pub use ironmill_core::ane::mil_text::*;
